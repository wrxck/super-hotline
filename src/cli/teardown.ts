export async function teardown(): Promise<void> {
  console.log(`
  super-hotline teardown — coming soon.

  To remove your deployment manually:
    - Cloudflare dashboard: Workers & Pages > super-hotline > Delete
    - Wrangler CLI: npx wrangler delete
  `);
}
